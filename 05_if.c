#include <stdio.h> //standard input-output header file library

int main()
{

    int num = 10;
    if (num > 10)
    {
        printf("numvalues is 10+%d\n", num);
    }

    int marks = 91;
    if (marks > 90)
    {
        printf("marks  is more 90%d\n", marks);
    }

    return 0;
}
