#include <stdio.h> //standard input-output header file library

int main()
{
    // rules variable name
    // a-z,A-Z, 0-9,_(Underscore)
    // can not start digit
    // num, NUM case sensetive
    // 32 ket wors not used like int return

    // declaretion
    int num;
    //  value assign
    num = 10;
    printf("%d \n", num);

    num = 15;
    printf("%d\n", num);

    num = 16;
    printf("%d\n", num);

    num = 18;
    printf("%d\n", num);

    num = 10;

    printf("%d\n", num);

    return 0;
}
